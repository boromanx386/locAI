PySide6==6.9.2
requests==2.31.0
feedparser
pyttsx3
pygame
edge-tts
# Kokoro TTS dependencies
kokoro>=0.9.4
soundfile>=0.12.0
numpy>=1.24.0
# Optional: for Japanese and Chinese support
# misaki[ja]  # For Japanese
# misaki[zh]  # For Chinese
asyncio
tempfile
TTS
torch
torchaudio
# Image processing dependencies
Pillow>=9.0.0
# Image generation dependencies
diffusers>=0.35.0
transformers>=4.56.0
accelerate>=1.10.0
# LoRA dependencies
peft>=0.7.0
# Upscaling dependencies (opcioni - za Real-ESRGAN upscaling)
# opencv-python>=4.8.0
# numpy>=1.24.0
# realesrgan>=0.3.0
# basicsr>=1.4.2

# Napomena: Za osnovne funkcionalnosti (SDXL, SD 2.1, SD 1.5, SD Upscaler)
# nisu potrebni dodatni dependencies. Real-ESRGAN upscaling je opcioni.
